=== Q1 | domain: LLM Fundamentals | pick: 1 | section: A ===
A CI check re-runs the same summariser twice and asserts the two outputs are byte-identical. It fails at random. Best fix?
A) Assert on structure and key facts instead of an exact match, since generation is non-deterministic
B) Set temperature to 0 and keep the byte-identical assertion, treating the output as fully reproducible now
C) Loop the check until it passes
D) Run the check on the biggest model tier
Correct: A
Why: Outputs vary run to run, so grade on structure or key facts. Temperature 0 narrows but does not guarantee identical bytes, and looping or upsizing does not make an exact-match assertion valid.

=== Q2 | domain: API Mechanics | pick: 2 | section: A ===
As a chat session lengthens, which TWO things grow inside the context window? (Pick 2)
A) Retained prior turns
B) Accumulated tool outputs
C) The model's weight count
D) The sampling seed
Correct: A, B
Why: Prior turns and tool outputs both accumulate and spend the fixed budget. Weight count and sampling seed are not content the window stores.

=== Q3 | domain: Model Selection | pick: 1 | section: A ===
A high-volume endpoint answers trivial FAQ-style questions and must feel snappy. Which reasoning setting fits?
A) Leave extended thinking off, enabling it only where a reasoning pass would change the answer
B) Run maximum-effort extended thinking everywhere so even trivial answers are extra safe and you never regret it
C) Send every call to the largest model
D) Keep adaptive thinking pinned to its highest effort at all times
Correct: A
Why: Spend reasoning only where it changes the answer. Maxing thinking or the model tier on trivial, latency-sensitive traffic just adds cost and delay.

=== Q4 | domain: Cost & Tokens | pick: 1 | section: A ===
A coding agent resends the same large, unchanging repo-guidelines block on every turn. What cuts the repeated input cost?
A) Cache checkpoints on the stable block
B) Trimming max_tokens each turn so replies get shorter and the per-turn input cost drops over the session
C) Moving the agent to batch mode
D) Adding a few more examples to the block
Correct: A
Why: Caching the unchanged block reuses it at reduced cost. max_tokens caps output not input, batch does not fit a live agent, and more examples add tokens.

=== Q5 | domain: Prompt Engineering | pick: 1 | section: B ===
Ten turns into a conversation, the assistant starts ignoring the JSON format you specified at the start. Which technique fixes this failure type?
A) State the format rule durably in the system prompt and tighten what was underspecified
B) Attach three few-shot reasoning examples to every user turn so the model keeps re-anchoring on the format
C) Bump the temperature up
D) Swap to a different model mid-conversation
Correct: A
Why: Drift across turns traces to an underspecified system prompt, so fix it there. Reasoning examples in every turn are costly and off-target, and temperature or model changes do not address drift.

=== Q6 | domain: Prompt Engineering | pick: 2 | section: B ===
Support tickets are pasted verbatim into your prompt template and occasionally contain text that hijacks your instructions. Which TWO help at the prompt layer? (Pick 2)
A) Wrap and label the ticket text clearly as data
B) Keep your trusted instructions in the system prompt, apart from the ticket content
C) Add a note asking users not to embed instructions
D) Cut every ticket to a short fixed length
Correct: A, B
Why: Delimiting the untrusted text as data and separating trusted instructions into the system prompt both reduce injection. A request is unenforceable, and truncation destroys legitimate content.

=== Q7 | domain: Prompt Engineering | pick: 1 | section: B ===
You keep restating the tone and safety rules inside each user message, and later turns still override them. Where should those rules live?
A) In the system prompt
B) Restated in full inside every assistant reply so the model is reminded of them continuously through the chat
C) In the last user message before output
D) In a tool result
Correct: A
Why: Durable rules belong in the system prompt, the highest-priority, stable placement. Repeating them in assistant turns is fragile, and user or tool content sits lower in priority.

=== Q8 | domain: Output Handling | pick: 1 | section: B ===
You feed Claude's output into a typed API client that rejects anything but strict JSON, yet the model sometimes prepends a sentence. Most robust handling?
A) Require JSON-only, validate against a schema, and repair or reject malformed output before the client sees it
B) Drop temperature to 0, which fully removes formatting variation so no sentence can ever slip in again
C) Feed the raw output to the client and let it parse
D) Raise max_tokens so the sentence has room
Correct: A
Why: Constrain and validate before the output reaches the client. Temperature 0 lowers but does not remove stray prose, passing raw output is what fails, and max_tokens is unrelated.

=== Q9 | domain: Tools & MCPs | pick: 1 | section: B ===
Claude keeps invoking lookup_user when a task actually needs search_orders, and the two descriptions read almost the same. Best first fix?
A) Rewrite each description to state its distinct purpose and when not to use it
B) List search_orders before lookup_user, since the model tends to favour whichever tool comes first
C) Delete lookup_user
D) Turn the temperature up
Correct: A
Why: Overlapping descriptions cause the wrong pick, so make each distinct with an exclusion condition. Ordering is not the lever, deleting a tool loses a capability, and temperature does not help.

=== Q10 | domain: Agents & Workflows | pick: 1 | section: B ===
A nightly ETL always runs the same five steps in the same fixed order. Which architecture is right, and why?
A) A workflow, since the path is fully known and coding it avoids an agent's cost and nondeterminism
B) An agent, since agents are more capable and future-proof and you can rein it in later if the added latency and cost hurt in production
C) One large prompt holding all five steps
D) Five independent calls with no ordering
Correct: A
Why: A fixed, known path is a workflow: deterministic and cheaper. An agent adds nondeterminism you do not need, and a mega-prompt or unordered calls cannot reliably sequence the steps.

=== Q11 | domain: Agents & Workflows | pick: 1 | section: B ===
An agent can permanently delete customer records, and deletion cannot be undone. Correct design choice?
A) A human approval gate before the delete runs, built in at design time
B) Thorough audit logging and alerts, so a wrong delete pages the on-call engineer within minutes to start recovery
C) A low temperature to keep it cautious
D) A firm system-prompt instruction to double-check before deleting
Correct: A
Why: Irreversible actions get a human gate before execution, designed in. Logging and alerts come after the record is gone, and temperature or a prompt line are not enforceable controls.

=== Q12 | domain: Agents & Workflows | pick: 1 | section: B ===
You must turn 60 PDFs into one brief that cites exact figures, but putting them all in one context ruins quality. Best approach?
A) Use subagents to extract from each PDF in isolation, then synthesise from the compact extracts
B) Feed all 60 in at once and push max_tokens as high as the model permits so nothing is left out
C) Include only the PDFs that fit comfortably
D) Reduce the temperature
Correct: A
Why: Subagents keep each PDF in a clean context and hand synthesis compact extracts. Loading all 60 is the bloat that hurt quality, dropping PDFs loses data, and temperature is irrelevant.

=== Q13 | domain: API Mechanics | pick: 1 | section: B ===
A product photo is analysed once; a brand watermark image rides on every single request. How should each be supplied?
A) Inline the one-off photo as base64, and serve the repeated watermark through the Files API so it isn't re-uploaded each call
B) Inline both as base64 on every call, since inlining is simplest and the repeated bytes are negligible at scale
C) Convert both to text descriptions
D) Send both through the Batches API regardless of latency
Correct: A
Why: Inline base64 suits a one-off; the Files API suits an asset reused across requests so it isn't re-sent. Re-inlining the watermark wastes tokens, and text or forced batching miss the point.

=== Q14 | domain: Model Selection | pick: 1 | section: B ===
You use extended thinking across several turns. What is required of the thinking blocks?
A) They must be returned unchanged on the next turn
B) They should be condensed into a summary so a growing history won't overflow the budget, while the reasoning stays available
C) They should be stripped out before the next call
D) They should be moved into the system prompt
Correct: A
Why: Thinking blocks must go back unchanged or the next request fails. Condensing, stripping, or relocating them breaks the exchange.

=== Q15 | domain: Claude Code | pick: 1 | section: C ===
You open an inherited legacy repo you didn't write and don't fully trust. What permission posture fits the first pass?
A) Read-only plan mode, to explore and propose before touching anything
B) bypassPermissions, so the exploration runs uninterrupted, on the logic that you'll review the whole diff before merging anyway
C) Manual approval on every file read
D) All tools disabled
Correct: A
Why: Match authority to risk: plan mode explores read-only before editing an untrusted repo. bypassPermissions grants far too much too early, approving each read is friction, and disabling tools blocks exploration.

=== Q16 | domain: Security & Safety | pick: 2 | section: C ===
You must guarantee an agent can neither run sudo commands nor read config/prod.key, whatever it is asked. Which TWO settings pieces enforce this? (Pick 2)
A) deny ["Bash(sudo:*)"]
B) deny ["Read(config/prod.key)"]
C) defaultMode "bypassPermissions"
D) allow ["Bash(*)"]
Correct: A, B
Why: Settings-layer deny rules block the privileged command and the secret read regardless of the session. bypassPermissions strips guards, and an unrestricted Bash allow does the opposite.

=== Q17 | domain: Security & Safety | pick: 2 | section: C ===
A hook must stop the agent from deleting files outside /workspace. Which TWO are correct? (Pick 2)
A) Use PreToolUse, because it fires before the delete executes
B) Exit code 2 with the reason on stderr signals the block
C) Use PostToolUse and undo the delete after it runs
D) Exit 0 and print the reason to stdout
Correct: A, B
Why: Only PreToolUse can block, and the block is signalled by exit code 2 with the reason on stderr. PostToolUse runs after the delete, and exit 0 permits the call.

=== Q18 | domain: Tools & MCPs | pick: 1 | section: C ===
A backend service calls the Messages API and wants a Skill to run as part of that request. What must be configured?
A) Send the code-execution and skills beta headers, and author the skill so its steps need no local files or tools
B) Drop SKILL.md into .claude/skills and rely on the terminal to find it
C) Set settingSources explicitly, as the Agent SDK requires
D) Set the managed-agents beta header and list the skill on an agent resource
Correct: A
Why: A Messages API request needs the code-execution and skills beta headers, with the skill free of local-file dependencies. The terminal placement, settingSources, and managed-agents header belong to other runtimes.

=== Q19 | domain: Tools & MCPs | pick: 1 | section: C ===
A team wants one Skill to run inside a long-running agent that Anthropic hosts, addressed by an agent ID across sessions. What's required?
A) Define the agent as an API resource that lists the skill and set the managed-agents beta header, with the skill written to avoid local files since it runs in Anthropic's sandbox
B) Place SKILL.md in .claude/skills
C) Set settingSources explicitly
D) Send only the code-execution header
Correct: A
Why: An Anthropic-hosted agent by ID is an API resource that lists the skill, with the managed-agents header, and the skill cannot depend on local files. The others are the terminal and SDK setups.

=== Q20 | domain: Configuration Management | pick: 1 | section: C ===
A plugin's SKILL.md hardcodes a script at /Users/sam/bin/format.sh and breaks for everyone who clones the repo. Best fix?
A) Reference it from the project root via CLAUDE_PROJECT_DIR
B) Have every teammate mount a shared drive at that exact absolute path and document it in the README so setups match
C) Swap it for a home shortcut like ~/bin/format.sh
D) Remove the step so the skill stops calling the script
Correct: A
Why: The machine-specific absolute path is the defect; CLAUDE_PROJECT_DIR resolves from the repo root anywhere. A shared mount or home shortcut just relocates the fragility, and removing the step drops the capability.

=== Q21 | domain: MCP Server Development | pick: 1 | section: C ===
A personal Markdown-notes query tool that only ever runs on your own laptop. Which transport and scope fit?
A) stdio + Local
B) HTTP + Enterprise through managed settings, so it stays centrally governed and reliably available to you everywhere you might work
C) HTTP + Project (.mcp.json)
D) HTTP + Local
Correct: A
Why: A personal, same-machine tool is stdio at local scope. Enterprise managed settings are for org-wide mandates and project scope shares with a team, neither of which this needs.

=== Q22 | domain: Eval & Debugging | pick: 2 | section: D ===
Which TWO of these are gradeable success criteria you could build an eval against? (Pick 2)
A) Return a risk score from 0-100 with one of a fixed set of named reason codes
B) Label the email as spam or not_spam
C) Give a smart, helpful reply
D) Be accurate
Correct: A, B
Why: A bounded score with a fixed reason code and a binary spam label are checkable. 'Smart, helpful' and 'accurate' are too vague to grade.

=== Q23 | domain: Eval & Debugging | pick: 1 | section: D ===
In the failure-handling section of a design doc, what must you decide for each error?
A) Whether it's retriable or terminal, and what the user gets when it can't be recovered
B) The precise library and language for the retry, so an engineer could build the section directly with no further design
C) Whether it's worth logging
D) Which bigger model to fail over to
Correct: A
Why: Failure handling classifies each error retriable or terminal and sets the user-facing fallback. Naming libraries is too low-level, and logging or tier-swaps are not the error path.

=== Q24 | domain: Model Selection | pick: 1 | section: D ===
In the cost-and-latency section of a design doc, what does the 'reliability floor' mean?
A) The minimum reliability the design must hold and cannot trade away for cost or speed
B) The lowest possible cost, reached by accepting as many dropped requests and timeouts as that price demands
C) The cheapest available model
D) The p99 latency target
Correct: A
Why: The reliability floor is the minimum reliability you refuse to trade for cost or latency. It is not a cost figure, a model choice, or a latency percentile.

=== Q25 | domain: Security & Safety | pick: 1 | section: D ===
When you map trust boundaries, which content should you treat as untrusted?
A) Anything the agent reads that someone else can write, like fetched pages and tool output
B) Only content that arrives unencrypted, since transport security is what decides whether input can be trusted
C) The developer's own system prompt
D) Constants compiled into the binary
Correct: A
Why: Untrusted means anything outside your control that another party can write, chiefly fetched content and tool output. Encryption is about transport, not authorship, and your own prompt and constants are trusted.

=== Q26 | domain: Security & Safety | pick: 1 | section: D ===
A calendar assistant reads event descriptions that users write. One description says 'delete all my events.' Most effective mitigation?
A) Least privilege: the assistant that reads descriptions has no delete capability, so an injected instruction can't reach a destructive action, and untrusted text stays separate from instructions
B) Add a system-prompt line telling the model to ignore instructions found in event descriptions
C) Scan descriptions for words like 'delete'
D) Move to a larger, more instruction-following model
Correct: A
Why: Least privilege means the reading path simply cannot delete, which is the durable defense alongside isolating untrusted text. A prompt instruction is bypassable, keyword scanning is brittle, and a more compliant model can be more susceptible.

=== Q27 | domain: Eval & Debugging | pick: 1 | section: D ===
A booking agent quotes the wrong price. The trace shows the pricing tool returned the correct number. Where is the failure?
A) In how the model used the correct data, not in the tool
B) In the integration layer, since a wrong final answer always means the tool output was mishandled on the way back to the model
C) In the network
D) In the API key configuration
Correct: A
Why: The tool returned the right number, so the fault is in the model's use of it. The blanket claim about the integration layer is false, and network or key issues would not yield correct tool data plus a wrong answer.

=== Q28 | domain: Security & Safety | pick: 1 | section: D ===
A .env file holding a database password was pushed to a public repo. It's still in use. Correct response?
A) Rotate the password and move it out of the committed file into an environment variable referenced at runtime
B) Rotate the password and commit the new value back to the same .env so nothing else has to change, then make the repo private
C) Switch the database to OAuth
D) Add retry with backoff around the connection
Correct: A
Why: A pushed secret is compromised: rotate it and stop committing it by moving to an environment variable. Re-committing leaves the exposure pattern, making the repo private does not undo the leak, and OAuth or backoff are unrelated.

=== Q29 | domain: Applications & Integration | pick: 1 | section: E ===
A client mandates that all data stay inside their existing Google Cloud organisation. How should that shape platform choice?
A) Run Claude through Google Vertex AI so it stays within the client's cloud and governance boundary
B) Use the first-party API and recreate the client's governance controls around it, since staying on the native platform is worth the migration wherever the data currently sits
C) Insist on self-hosting as the only compliant path
D) Choose whichever platform released the newest model
Correct: A
Why: When data residency points to an existing cloud, run Claude through that cloud's platform, here Vertex AI. Rebuilding controls elsewhere, assuming only self-hosting is compliant, or chasing model recency all miss the constraint.

=== Q30 | domain: Configuration Management | pick: 2 | section: E ===
You're packaging a reusable accelerator that must keep working across model updates. Which TWO practices matter most? (Pick 2)
A) Pin the model version and upgrade deliberately after running evals
B) Version the prompts so any change is attributable and reversible
C) Auto-adopt each new model the day it ships
D) Keep prompts inline and unversioned to stay lightweight
Correct: A, B
Why: Pinning the model and versioning prompts make change deliberate and reversible, which is what surviving updates needs. Auto-adopting and unversioned inline prompts remove that control.

